---
name: one-for-all
description: >-
  Project knowledge graph guide for {{ project_name }}.
  Provides project structure, corpus reference, key enriched symbols,
  and current Power Level. Auto-loaded when agent needs to understand the project.
---

# One For All — {{ project_name }}

> **All-Might** manages the corpora that index this codebase, providing semantic search,
> enrichment, and graph intelligence.
>
> All knowledge graph operations go through **All-Might commands** (`/search`, `/enrich`, `/explain`, etc.).
> Do NOT hand-edit sidecar or config YAML files.
>
> Generated at: {{ generated_at }}

## 1. Project Overview

- **Name**: {{ project_name }}
{% if languages %}
- **Languages**: {{ languages | join(', ') }}
{% endif %}
{% if frameworks %}
- **Frameworks**: {{ frameworks | join(', ') }}
{% endif %}

## 2. Corpus Reference

| Index | Description | Paths |
|-------|-------------|-------|
{% for idx in indices %}
| `{{ idx.name }}` | {{ idx.description }} | `{{ idx.paths | join(', ') }}` |
{% endfor %}

Use `/list-indices` to verify indices are active.

> **Note:** This workspace is a standalone hub — source code is at external paths
> listed in the "Paths" column above. Search data stored locally in `./smak/`.
> Sidecar files live beside the source files at those external paths, not here.
> **Corpora are built from online (Layer 1) only.** To verify features in version control
> releases, use SOS revision log matching (see `sos-smak` skill).

### Choosing the right index

{% for idx in indices %}
- **`{{ idx.name }}`** — {{ idx.description }}
{% endfor %}

When unsure which index, use `/search "<query>"` to search the default index.

## 3. Key Symbols

{% if key_symbols %}
The following symbols have been enriched with intent and/or relations:

| Symbol | File | Intent | Relations |
|--------|------|--------|-----------|
{% for sym in key_symbols %}
| `{{ sym.name }}` | `{{ sym.file_path }}` | {{ sym.intent[:80] }}{% if sym.intent|length > 80 %}...{% endif %} | {{ sym.relation_count }} |
{% endfor %}

These are your best starting points for understanding the codebase.
Use `/explain "<uid>"` to see full graph context for any symbol.
{% else %}
> No symbols have been enriched yet. As you work with this project and use
> `/enrich` to annotate code, this section will be populated when you
> run `/regenerate`.
{% endif %}

## 4. Power Level

- **Coverage**: {{ "%.1f" | format(power_level.coverage_pct) }}%
- **Enriched symbols**: {{ power_level.enriched_symbols }} / {{ power_level.total_symbols }}
- **Total relations**: {{ power_level.total_relations }}

{% if power_level.by_index %}
### Coverage by Index

| Index | Coverage |
|-------|----------|
{% for name, pct in power_level.by_index.items() %}
| `{{ name }}` | {{ "%.1f" | format(pct) }}% |
{% endfor %}
{% endif %}

Run `/power-level` to get fresh metrics, or `/regenerate` to update this skill.

## 5. All-Might Commands

| Command | Purpose |
|---------|---------|
| `/search <query>` | Semantic search with graph context |
| `/explain <uid>` | Full graph context for a symbol |
| `/enrich` | Annotate a symbol with intent/relations |
| `/ingest` | Rebuild corpus |
| `/power-level` | Check knowledge coverage metrics |
| `/regenerate` | Update this skill with latest state |
| `/panorama` | Export knowledge graph visualization |
| `/graph-report` | Graph intelligence report |
| `/add-index` | Add a new corpus |
| `/remove-index` | Remove an index |
| `/list-indices` | List all configured indices |

## 6. Getting Started

1. Use `/search "..."` to explore the codebase semantically
2. Use `/explain "path::symbol"` for deep context on any symbol
3. When you understand a symbol's purpose, use `/enrich` (see `enrichment-protocol` skill)
4. Run `/power-level` periodically to track progress
5. Run `/regenerate` after significant enrichment to update this skill
